# Backend (Django)

This folder is intended for the Django project and apps.

Suggested next steps:
1. Create and activate a virtual environment.
2. Install dependencies from `requirements.txt`.
3. Run: `django-admin startproject dfs_site .` **inside** `backend/` (or similar).
4. Add `nba`, `nfl`, and `common` apps under `backend/apps/`.
